rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sementara untuk development — allow all read/write
    // GANTI ini sebelum production deployment!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
